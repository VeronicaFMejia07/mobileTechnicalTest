import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    containerNotFound:{
        justifyContent: 'center',
        alignItems: 'center',
        marginVertical: 20,
        height: 500
    },
    title:{
        fontSize: 20,
        marginTop: 15,
        color: '#400D51'
    }
});